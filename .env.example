# PostgreSQL (docker-compose)
POSTGRES_USER=ifome
POSTGRES_PASSWORD=ifome
POSTGRES_DB=ifome
POSTGRES_PORT=5432

# Prisma
DATABASE_URL="postgresql://ifome:ifome@localhost:5432/ifome?schema=public"

# Auth / JWT
JWT_SECRET="troque-este-segredo-em-producao"
